{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open app in Chrome (localhost:8080)",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
